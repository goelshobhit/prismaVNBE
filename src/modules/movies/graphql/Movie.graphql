type Movie {
  id: ID!
  name: String
  description: String
  directorName: String
  releaseDate: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

extend type Query {
  movies(filter: String,offset: Int, limit: Int, orderKey: String, orderDirection: SortDirection): [Movie]
}

